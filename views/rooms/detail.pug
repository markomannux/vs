extends ../layouts/operator

block meta
  meta(name='page', content='waiting-list')
  meta(name='room-id', content=roomId)

block scripts
  script(src='/javascripts/waiting-list.bundle.js', defer)

block content
  h1= title
  video(autoplay, playsinline, controls, muted)
  video(autoplay, playsinline, controls, muted)

  button#master-button Start master
  button#stop-master-button Stop master


  h2 In corso
  h2 Sala d'attesa
    ul(data-behavior='waiting-list')
      include fragments/waiting_list
  h2 Pianificate
    ul
      each appointment in planned
        li= appointment.summary

  script(src="/javascripts/kinesis.bundle.js")